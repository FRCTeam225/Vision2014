<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title></title>

    <!-- Bootstrap -->
    <link href="static/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  	<div class="container">
    		<a class="navbar-brand" href="#">Team 225</a>
	        <ul class="nav navbar-nav">
	                <li><a href="/vision">Vision</a></li>
	                <li><a href="/networktable">NetworkTable</a></li>
	        </ul>
  	</div>
    </nav>
    <br><br><br><br>
    <div class="container">
		<img src="/target.mjpeg"></img>
		<br><br>
		<button class="btn" id="showTuning">Edit Tuning Constants</button>
		<table class="table" id="tuning">
			<thead>
				<tr>
					<th>Label</th>
					<th></th>
					<th>Value</th>
				</tr>
			</thead>
			
			<tbody>
				<tr>
					<th>H High</th>
					<td><input type="range" class="thresh" name="highH" min="0" max="180" value="<%= config.highH %>"></td>
					<td id="highHvalue"><%= config.highH %></td>
				</tr>
				
				<tr>
					<th>H Low</th>
					<td><input type="range" class="thresh" name="lowH" min="0" max="180" value="<%= config.lowH %>"></td>
					<td id="lowHvalue"><%= config.lowH %></td>
				</tr>
				
				<tr>
					<td></td>
					<td></td>
					<td></td>
				</tr>
				
				<tr>
					<th>S High</th>
					<td><input type="range" class="thresh" name="highS" min="0" max="180" value="<%= config.highS %>"></td>
					<td id="highSvalue"><%= config.highS %></td>
				</tr>
				
				<tr>
					<th>S Low</th>
					<td><input type="range" class="thresh" name="lowS" min="0" max="180" value="<%= config.lowS %>"></td>
					<td id="lowSvalue"><%= config.lowS %></td>
				</tr>
				
				<tr>
					<td></td>
					<td></td>
					<td></td>
				</tr>
				
				
				<tr>
					<th>V High</th>
					<td><input type="range" class="thresh" name="highV" min="0" max="180" value="<%= config.highV %>"></td>
					<td id="highVvalue"><%= config.highV %></td>
				</tr>
				
				<tr>
					<th>V Low</th>
					<td><input type="range" class="thresh" name="lowV" min="0" max="180" value="<%= config.lowV %>"></td>
					<td id="lowVvalue"><%= config.lowV %></td>
				</tr>
			</tbody>
		</table>
    </div>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="static/jquery.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="static/js/bootstrap.min.js"></script>
	
	<script type="text/javascript">
		$("#save").click(function()
		{
			$.get("/save");
		});

		$(".thresh").change(function() {
			propertyName = $(this).attr("name");
			propertyValue = $(this).val();
			console.log(propertyName+" is now "+propertyValue); 
			$.get("/setProperty/"+propertyName+"/"+propertyValue);	
			$("#"+propertyName+"value").text(propertyValue);	
		});

		$("#tuning").hide();
		$("#showTuning").click(function()
		{
			$("#tuning").show();
			$("#showTurning").hide();
		});
	</script>
	
  </body>
</html>

